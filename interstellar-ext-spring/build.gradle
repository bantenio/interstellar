
plugins {
    id 'org.tenio.java-conventions'
}

dependencies {
    api project(":${project_root_name}-${sub_ext_jackson}")
    compileOnlyApi group: 'org.springframework', name: 'spring-expression', version: "${SpringVersion}"
}
