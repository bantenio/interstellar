
plugins {
    id 'org.tenio.java-conventions'
}

dependencies {
    api project(":${project_root_name}-${sub_core}")

    compileOnlyApi group: 'io.projectreactor', name: 'reactor-core', version: "${ReactorVersion}"
}
